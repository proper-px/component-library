import React from "react";
import styled from "styled-components";

const Styled{{componentName}} = styled.{{element}}`
`;

interface I{{componentName}}Props {
  label: string;
}

export const {{componentName}} = ({ label }: I{{componentName}}Props) => {
  return <Styled{{componentName}}>{label}</Styled{{componentName}}>;
};
