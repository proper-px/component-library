import { DefaultTheme } from "styled-components";

const Theme: DefaultTheme = {
  button: {
    padding: "6px 10px",
    borderWidth: "1px",
    borderRadius: "4px",
    borderStyle: "solid",
  },
};

export { Theme };
